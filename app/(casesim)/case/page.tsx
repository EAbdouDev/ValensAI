import SoundReactiveAnimation from "@/components/cases/SoundAnimation";
import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <div>
      <SoundReactiveAnimation />
    </div>
  );
};

export default page;
